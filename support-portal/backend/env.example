AWS_REGION=us-east-1
AWS_OFFLINE=true
S3_BUCKET_NAME=support-portal-uploads

# Local fallback user (overriden by X-User-Id header)
DEFAULT_USER_ID=demo-user

# RDS credentials (leave empty to fall back to local SQLite)
DB_HOST=
DB_PORT=5432
DB_NAME=support_portal
DB_USER=
DB_PASSWORD=

# Optional AWS credentials for S3 uploads
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

